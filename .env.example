# Copy to .env.local for Vite. In Vercel: Project Settings → Environment Variables (same names).

# Supabase → Project Settings → API
# Project URL
VITE_SUPABASE_URL=

# public anon key (safe in the browser with RLS)
VITE_SUPABASE_ANON_KEY=
